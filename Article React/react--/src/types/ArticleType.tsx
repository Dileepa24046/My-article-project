// Assuming ArticleType looks something like this:
interface ArticleType {
  id: number;
  title: string;
  description: string;
  createDate: string;
  autherId: number;
}

export default ArticleType;
